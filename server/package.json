{
    "name": "smart-brand-stylist-api",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "release": "node release.mjs"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
