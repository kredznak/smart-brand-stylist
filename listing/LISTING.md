# Adobe listing content

Everything the submission form asks for, written out so it can be pasted in. The form is
organised into the tabs below; each heading here matches one.

The form counts characters for you and shows the remaining count as you type. Only two
limits are published: the public name is **25 characters** and the payment details field
is **250**. Everything else here is kept short on purpose, so nothing needs cutting at
the keyboard.

---

## Listing details > General

**Public add-on name** (25 max)

```
Smart Brand Assistant
```

19 characters. It says what the add-on does and is distinct enough to search for.

**Subtitle**

```
Turn a logo or website into a brand kit
```

**Support email**

```
kredznak@gmail.com
```

Not shown to users. Adobe uses it to reach the publisher.

**Help URL**

```
https://kredznak.github.io/smart-brand-stylist/help.html
```

Generated from `HELP.md` by `npm run pages`. Walks through each tab and answers the
common questions (fonts missing, website not readable, daily limit).

**Description**

```
Smart Brand Assistant builds a brand kit from what you already have, then keeps the
rest of your design in line with it.

Start from a logo or a website address. Upload a logo and the colors are read on
your device, in the panel — the image is never uploaded anywhere. Type a website
address instead and the add-on reads that page's stylesheets and returns the colors
and font families it actually uses.

What you get:

COLORS — A palette pulled from your source, plus a picker and five harmonies
(complementary, analogous, triadic, split complementary, monochrome) to explore
variations. Click any swatch to apply it to the selection, or drop the whole
palette onto the page.

FONTS — Heading and body fonts with suggested pairings, applied to selected text
in a click. Scans the page for fonts that aren't yours and swaps them.

COPY — Headlines, taglines and body copy written to your brand voice, added to
the document in a click.

AUDIT — Scans the page for off-brand colors, shows where each one is used and
which brand color it would become, and fixes them all in one step. A tolerance
slider decides how close counts as close enough. Every change is a normal edit,
so Cmd/Ctrl+Z undoes it.

Copy suggestions and logo font matching are generated by Claude (Anthropic). Nothing
you write or upload is stored — see the privacy policy for exactly what is sent and
what is kept.
```

Written as an elevator pitch, as the listing guidelines ask: what it does first, then
the four tabs, then the AI disclosure. The claim that logo colors never leave the device
is true — `src/ui/extractColors.ts` runs in the panel.

That version is 1,379 characters. Adobe does not publish a limit for this field — the
counter in the form is the only place it appears — so if it turns out to be shorter, use
this one instead rather than trimming on the spot. It keeps the AI disclosure and the
on-device claim, which are the two parts that should not be cut:

```
Build a brand kit from what you already have, then keep the rest of your design in
line with it.

Start from a logo or a website address. Upload a logo and the colors are read in the
panel, on your device — the image is never uploaded anywhere. Type a website address
and the add-on reads that page's stylesheets and returns the colors and fonts it
actually uses.

COLORS — Your palette, plus five harmonies to explore variations. Click a swatch to
apply it to the selection.
FONTS — Heading and body fonts with suggested pairings, applied to selected text.
COPY — Headlines, taglines and body copy in your brand voice.
AUDIT — Finds colors and fonts that are off brand and fixes them in one step.
Cmd/Ctrl+Z undoes any change.

Copy suggestions and logo font matching are generated by Claude (Anthropic). Nothing
you write or upload is stored — see the privacy policy for what is sent and kept.
```

## Listing details > Media

**Icon** — upload `icon-144.png`.

Adobe takes a single 144 px icon and resizes it into the 36 px minimized icon and the
64 px panel header icon itself. `icon-36.png` and `icon-64.png` are kept for checking
how the mark holds up small, not for uploading.

## Listing details > Tags

**Categories** — the form's own dropdown is the only place the full list appears; it is
not in Adobe's published docs. Pick the ones that fit, in this order of preference:

1. Design / Graphics — the add-on's core job is applying color and type
2. Productivity — the audit and one-step fixes
3. AI — only if the list has such a category; the AI is one tab of four

**Custom tags** — spelled out in full, as the guidelines require, and chosen for what
someone would actually type:

```
brand kit
brand colors
color palette
brand fonts
font pairing
logo colors
brand guidelines
color picker
```

## Listing details > Services

**Privacy policy URL**

```
https://kredznak.github.io/smart-brand-stylist/
```

Generated from `PRIVACY.md` by `npm run privacy`. Verified public and returning 200.

**Terms of service URL**

```
https://kredznak.github.io/smart-brand-stylist/terms.html
```

Generated from `TERMS.md` by `npm run pages`.

**Commerce method** — Free.

---

## Version details > General

**Add-on package** — `dist.zip`, from `npm run package`. Around 66 KB.

**Application requirements** — Adobe Express. No other Adobe app.

**Third-party service requirements** — declare these; both are real and both are reached
from the add-on:

```
The add-on calls a small helper server run by the publisher
(smart-brand-stylist-api.red-surf-5c86.workers.dev, hosted on Cloudflare Workers).

That server does two things:

1. Reads a public website that the user types in, and returns the colors and font
   families found in its stylesheets. No AI and no account are involved.

2. Forwards copy-suggestion and font-matching requests to the Anthropic Claude API
   (api.anthropic.com) and returns the reply. The API key lives on the server and is
   never in the add-on.

No user account is required and nothing is stored. Daily per-caller limits apply.
```

**Supported UI languages** — English.

**Release notes** (version 1.0.0)

```
First release.

- Build a brand kit from a logo, read on your device, or from a website address
- Palette with color harmonies, applied to a selection or to the whole page
- Heading and body fonts with suggested pairings, applied to selected text
- AI copy suggestions in your brand voice, placed into the document
- Audit the page for off-brand colors, and off-brand fonts, and fix them in one step
```

## Version details > Media

**Screenshots** — at least one is required. PNG or JPG at **1360 x 800**, which is the
size Adobe recommends. The panel is much narrower than that, so each shot is the Express
window with the panel open on the right, not the panel cropped out on its own — that also
shows the add-on doing something to a real document, which is what the guidelines ask for.

Five shots, in the order they should appear:

| # | Screen | What has to be visible |
| --- | --- | --- |
| 1 | Upload screen | Both halves: the logo picker and the website field. This is the first thing a new user meets, so it leads. |
| 2 | Colors tab | The palette after analysing, a swatch mid-apply, and the shape on canvas that just took the color. |
| 3 | Fonts tab | Heading and body fonts with a pairing suggestion, and text on canvas in that font. The font scan can share this shot if it fits. |
| 4 | Audit tab | A scan result with off-brand swatches listed, each showing the brand color it would become, and the fix button in view. |
| 5 | Copy tab | Generated headline and body options next to the document they were written for. |

Use a real document, not an empty canvas. Shot 4 needs a page that genuinely has
off-brand colors on it, so set that up before capturing.

**Videos** — optional, and skipped for the first release.

---

## Before the first submission

The publisher profile has to be completed and submitted before any add-on can be. It
asks for a 250 x 250 logo: upload `publisher-logo-250.png`. If the add-on is to be
available in the EU, trader details (email, telephone, address) are required too.

**Notes to reviewer** are in `REVIEWER-NOTES.md`; paste them into the last step of the form.
